let a = 100;
let b = 250;
let c = a * b;
let d = c / 50 + 40;

func greet(name) {
  -- this is so cool! 
  let msg = "hello there " .. name;

  if (true) {
    let poop = "kak";
  };

  return msg;
};

let returned_message = greet("julia");
