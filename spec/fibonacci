 let fibonacci = func(n) {
   return if n == 0 {
     0
   } else {
     if n == 1 {
       1
     } else {
        -- FIX: cannot do inline return for binary expressions
        let next = fibonacci(n-1) + fibonacci(n-2);
        next
     }
   };
 };

dbg(fibonacci(4))
